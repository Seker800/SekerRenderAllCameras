# 文档架构维护原则

文档按稳定性和用途分层：长期原则进入 `architecture/`，实现事实进入 `reference/`，操作步骤进入 `workflow/`，工具说明进入 `tools/`，设计原因进入 `我的决策/`。

入口保持简短，细节下沉至权威位置。计划、日志、实验和 review 默认不是长期真相源；其中稳定结论必须提炼并迁回主干文档。根 `PLAN.md` 是当前产品需求基线，但实现路径、版本事实和工具命令仍分别归属架构、参考和工具文档。
